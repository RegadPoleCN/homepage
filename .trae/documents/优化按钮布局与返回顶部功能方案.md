根据您的要求，我将对设置按钮和返回顶部按钮进行以下优化，重点在于美化样式和主题适配：

## 1. 桌面端设置按钮优化
- **位置调整**：将设置按钮移动到 [App.vue](file:///c:/Users/RegadPole/Desktop/homepage/src/App.vue) 的 `.center-column` 容器内。
- **定位逻辑**：
  - 在桌面端使用 `absolute` 定位，相对于中栏右上角对齐。
  - 在移动端使用 `fixed` 定位，固定在屏幕右上角，确保随时可用。

## 2. 移动端火箭返回顶部按钮 (美化版)
- **视觉设计**：
  - **图标**：选用 `mdi:rocket-launch` (火箭发射) 图标，动感十足。
  - **造型**：采用 16px 圆角的“平滑方圆” (Squircle) 造型，比纯圆形更具设计感。
  - **背景**：使用主题色 `var(--primary-color)`，并添加轻微的毛玻璃效果 (`backdrop-filter`)。
  - **阴影**：添加具有深度感的阴影效果，悬浮感更强。
- **主题适配**：
  - 核心颜色（背景、阴影、图标色）全部绑定 CSS 变量（`--primary-color`, `--text-color` 等）。
  - 当您在设置中切换“暖阳”、“深邃”或“清新”主题时，按钮颜色会随之丝滑切换。
- **交互动画**：
  - **显隐**：使用 `scale` 和 `opacity` 配合弹性贝塞尔曲线 (`cubic-bezier`)，使按钮出现时带有轻微的弹跳感。
  - **悬停**：鼠标悬停时（若在移动端模拟点击前）按钮会轻微上浮并放大。
- **布局位置**：位于左下角 `(bottom: 2rem; left: 2rem)`，仅在移动端滚动超过 `500px` 后显示。

## 3. 技术实施
- 在 [App.vue](file:///c:/Users/RegadPole/Desktop/homepage/src/App.vue) 中集成 `handleScroll` 逻辑。
- 优化 CSS 结构，利用现有的主题系统确保全自动适配。

请确认此方案，我将为您打造更精致的交互体验。