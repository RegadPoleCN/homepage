根据您的要求，我计划进行以下调整：

## 1. 电脑端设置按钮
- **位置调整**：将当前固定在屏幕右上角的设置按钮（`.settings-btn`）移至中栏（`center-column`）的右上角。
- **保留功能**：按钮依然使用齿轮图标（`mdi:cog`），功能和交互不变。

## 2. 手机端新增悬浮按钮
- **新增功能**：在手机端（`768px` 以下）适配上，除了现有的 `mobile-footer`，新增一个“回到顶部”的悬浮按钮。
- **图标与样式**：该按钮将使用通用的“箭头向上”图标（如 `mdi:arrow-up`），并设置合适的尺寸和阴影，使其在移动端页面较长时方便用户快速回到顶部。

## 3. 实现方法
- **App.vue 调整**：
    - 修改 `.settings-btn` 的定位和容器，使其在桌面端固定于中栏。
    - 在 `768px` 媒体查询中，为新增的“回到顶部”按钮添加样式和布局。
- **交互逻辑**：通过 CSS 的 `position: sticky` 或 JavaScript 的 `scroll` 事件，确保返回顶部按钮的动态行为。

您同意这个变更方案吗？