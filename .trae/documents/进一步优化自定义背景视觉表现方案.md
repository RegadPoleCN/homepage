我将根据您的反馈，进一步优化自定义背景主题的视觉体验：

## 1. 优化主题列表图标
- **更换图标**：在 [SettingsModal.vue](file:///c:/Users/RegadPole/Desktop/homepage/src/components/SettingsModal.vue) 中，为“自定义背景”主题预览块添加一个更直观的图标（如 `mdi:image-plus` 或 `mdi:wallpaper`），以便一眼认出。
- **视觉优化**：调整预览块内的图标显示逻辑，确保在未选中时也能通过图标识别出这是自定义背景选项。

## 2. 降低遮挡感（增加透明度）
- **调优色值**：在 [site.config.json](file:///c:/Users/RegadPole/Desktop/homepage/src/config/site.config.json) 中，进一步降低自定义主题各面板及卡片的背景不透明度。将 alpha 通道值从目前的 `0.45-0.65` 降低到 `0.15-0.3` 左右，使背景图更加清晰可见。

## 3. 减弱毛玻璃效果
- **调整模糊度**：在 [useTheme.ts](file:///c:/Users/RegadPole/Desktop/homepage/src/composables/useTheme.ts) 中，将自定义主题触发的 `--backdrop-blur` 变量从 `20px` 降低至 `8px` 左右。这样既能保留磨砂质感来区分层级，又不会过度模糊背景细节。

## 4. 预期效果
- 设置菜单中的自定义选项更具辨识度。
- 开启自定义背景后，三栏面板将呈现出一种轻盈、半透明的质感，完美衬托出背景图片的细节，同时保证文字内容的可读性。

您是否同意按照此优化方案进行调整？