根据您的最新要求，我再次优化了方案：

## 1. 设置按钮布局 (桌面端)
- 从全局固定位置 `(top:1.5rem; right: 1.5rem)` 调整为附着于 `.center-column` 右上角区域。
- 保留 `cog` 齿轮图标作为主设置入口。

## 2. 返回顶部按钮 (移动端)
- **图标选择**：选用 `mdi:rocket-launch` 或 `mdi:send` 图标，替代箭头图标，更具视觉吸引力。
- **样式设定**：
  - 圆形悬浮按钮，尺寸为 `50px`。
  - 使用 `var(--primary-color)` 作为背景色，白色图标。
  - 通过 `transform: scale(0)` 初始隐藏，滑动时 `scale(1)` 出现。
  - 位于视口左下角（`bottom: 1.5rem; left: 1.5rem`），并保持 `position: fixed`。
  - 添加阴影与悬停效果以增强可操作性。

## 3. 响应式逻辑
- 在 [App.vue](file:///c:/Users/RegadPole/Desktop/homepage/src/App.vue) 中集成上述样式，并通过 JavaScript 检测滚动状态决定移动端按钮的显现（例如滚动 `500px` 后显示）。

请审阅调整后的方案，我将根据您的意见继续推进。